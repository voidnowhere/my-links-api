server:
  port: 0
spring:
  application:
    name: links-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/links-service
    username: root
    password:
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka